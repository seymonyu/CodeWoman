import React from 'react'

function Question({handlerunmountQuestion}) {
    return(

        <div>
        <h1>I AM QUESTION.JS</h1>
        <button onClick={handlerunmountQuestion}>Next</button>
        </div>
    )
}

export default Question